<% if user_signed_in? %>
  <li>

<%= link_to(
      content_tag(
        :i,
        nil, 
        class: 'fa fa-sign-out'
      ), 
      destroy_user_session_path,
      method: :delete,
      class:
        "btn small waves-effect waves-light red" 
    ) 
%>
  </li>	
<% end %>